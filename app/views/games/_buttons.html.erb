<%= render(
   partial: 'button',
   locals: {
     data_action: 'click->shuffle#shuffleTiles',
     disabled: disabled,
     label: t('games.show.shuffle')
   }
) %>
<%= render(
   partial: 'button',
   locals: {
     data_action: 'click->submit#playTurn',
     disabled: disabled,
     label: t('games.show.submit_word')
   }
) %>
<%= render(
   partial: 'button',
   locals: {
     data_action: 'click->modal#showSkipModal',
     disabled: disabled,
     label: t('games.show.skip_turn')
   }
) %>
