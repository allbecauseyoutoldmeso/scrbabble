<%= form_with model: Game.new do |form| %>
  <%= form.label :other_user_id %>
  <%= form.select(
    :other_user_id,
    @other_users.map { |user| [user.name, user.id] },
    {},
    { class: 'select' }
  ) %>
  <%= form.submit(t('helpers.submit.game.create'), class: 'button') %>
<% end %>
